<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>PDF Report</title>
  <style>
    body {
      font-family: "Arial", sans-serif;
    }
    .header {
      text-align: center;
      font-size: 20px;
      margin-bottom: 20px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    table, th, td {
      border: 1px solid black;
    }
    th, td {
      padding: 10px;
      text-align: left;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Group Report for <%= @group.name %></h1>
  </div>

  <h2>Members:</h2>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
      <% @group.members.each do |member| %>
        <tr>
          <td><%= member.user.username %></td>
          <td><%= member.user.email %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
